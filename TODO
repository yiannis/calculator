TODO

llvm:
 0. input vars in main()
 1. IR optimization
 2. Compiler::result() should not JIT the code
 3. Test benchmarks
 4. Convert llvm doubles to floats

general:
0. Add *_BEGIN/*_END in TokenType and create static isOpType() functions
1. Really use Exceptions, not just throw 'int'
2. Create destructors for ExprBase, etc and Parser!!!

- Add pi/e/... constants
- Replace 'char m_op' with 'TokenType m_op' [OK]
- Support ++, --,... [WRONG]: x,y are immutable
- Handle unary '-' [OK]
